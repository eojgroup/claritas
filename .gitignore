# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*

# macOS
.DS_Store

# Xcode user state
**/xcuserdata/
**/*.xcuserstate

# Apple signing and push credentials are deployment secrets, never source.
AuthKey_*.p8
aps*.cer
*.p12
*.mobileprovision

# JavaScript dependencies and build caches
**/node_modules/
**/dist/
# Download Cloud SQL Auth Proxy binaries locally when needed; never commit them.
/cloud-sql-proxy
